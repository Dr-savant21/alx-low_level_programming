#include "main.h"

/**
 * mul - multiplies two numbers.
 * author: Dr-savantcode
 * @a: first number.
 * @b: second number.
 * Return: result.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
