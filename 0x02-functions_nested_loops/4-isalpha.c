#include "main.h"
/**
 * isalpha - fuction to check if c is a letter, lowercase or uppercase
 * @c: is the int that will use for argument of the function
 * author: Dr-savantcode
 * Return: 0 success
 */
int isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
	return (0);
	}
	else
	return (0);
}
